{
  "name": "customers-service",
  "packageManager": "yarn@4.0.1",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "start": "ts-node index.ts",
    "build": "tsc",
    "pull-submodules": "git submodule update --init --recursive",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "ffi-napi": "^4.0.3",
    "ref-napi": "^3.0.3",
    "ref-struct-di": "^1.1.1"
  },
  "devDependencies": {
    "@types/ffi-napi": "^4",
    "@types/ref-napi": "^3",
    "@types/ref-struct-di": "^1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
